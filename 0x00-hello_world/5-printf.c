#include <stdio>

/**
 * main - Entry point
 *
 * Description: the programs prints a string using printf
 * Return: Always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
